// Game constants: enums and immutable objects

const PieceType = Object.freeze({
  REGULAR: 'regular',
  PUSHER: 'pusher',
  YELLOW: 'yellow'
});

const Player = Object.freeze({
  P1: 1,
  P2: 2
});
